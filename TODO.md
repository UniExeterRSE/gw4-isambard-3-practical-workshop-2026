# Notes

- consider “portability” of the workshop. E.g. while I use pixi here, add a pixi task to batch export to environment.yml
- [ ] test ABI swap of MPI with conda env
- [ ] consider having a QR JS lib to show current URL at top right hand corner
- [ ] create a fallback stack at project
- [ ] section 5 be careful to load the conda environment on compute node
  - investigate `${MAMBA_ROOT_PREFIX}/etc/profile.d/mamba.sh`
- [ ] add a C example for advanced users
